# 安全政策

## 支持范围

安全更新仅针对 `main` 分支的最新版本。请在部署前使用自己生成的密钥，并保持依赖与操作系统更新。

## 报告漏洞

请不要在公开 Issue 中提交可利用细节、Token、数据库、二维码或微信标识。请通过 GitHub 仓库的 **Security → Report a vulnerability** 私密报告，并包含：

- 受影响版本和组件；
- 最小复现步骤；
- 影响评估和建议修复方向；
- 是否已在第三方公开。

我们会尽快确认报告，并在修复发布前协调披露时间。

## 部署基线

- 绝不要提交 `.env`、数据库、日志、上传文件或 Bot 凭据。
- 生产环境使用 HTTPS，并将 Uvicorn 限制在回环地址。
- 为每个公司和外部应用分配独立最小权限 Token。
- 定期备份 SQLite 与加密密钥，并测试恢复。
- 仅在确认 Hermes Weixin 适配器来源与版本后启用真实投递。
